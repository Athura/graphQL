import React from 'react';
import Header from './Header';

export default () => {

    return (    
        <div>
            <Header />
            You are logged in onto the dashboard!
        </div>
    );
};